const User = require('./User');
const Cohort = require('./Cohort');
const Student = require('./Student');
const Note = require('./Note');
const Groups = require('./Groups');

module.exports = { User, Cohort, Student, Note, Groups };
