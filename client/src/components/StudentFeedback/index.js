import React from 'react'

export default function StudentFeedback() {
    return (
        <div className="StudentFeedback">
            <h1>Feedback!</h1>
            <p className="lead">This will be a space to view weekly student feedback - the API is usually on week behind the current cohort week, so feedback will be outdated but still useful!</p>
            <p className="text-danger">Not Yet Functional</p>
        </div>
    )
}
